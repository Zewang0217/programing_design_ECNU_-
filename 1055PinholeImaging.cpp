/*
#include <iostream>
#include <iomanip>

using namespace std;

int main() {
	int T;
	cin >> T;

	for (int t = 0; t < T; t++) {
		int A, B, H;
		cin >> A >> B >> H;
		double height;
		double rate = (double)B / (double)A;
		height = (double)H * rate;
		cout << fixed << setprecision(2) << height << endl;
	}
}
*/